// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Poppins'], // Use Poppins as the default sans font
      },
    },
  },
  // Other Tailwind configuration settings
};